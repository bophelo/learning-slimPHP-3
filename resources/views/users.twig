{% extends 'layouts/app.twig' %}

{% block title %}
    Users Page
{% endblock %}

{% block content %}
    <ul>
        {% for user in users %}
            <li>{{ user.username }}</li>
            <li>{{ user.name }}</li>
            <li>{{ user.email }}</li>
        {% endfor %}
    </ul>
{% endblock %}